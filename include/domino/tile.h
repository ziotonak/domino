/**
 * @file tile.h
 * @brief Definition of the tile structure.
 *
 * This file defines a simple structure representing a tile with two unsigned integers.
 */

#pragma once

/**
 * @struct tile_t
 * @brief Structure representing a tile with two unsigned integers.
 */
typedef struct {
    unsigned first;    /**< First unsigned integer of the tile. */
    unsigned second;   /**< Second unsigned integer of the tile. */
} tile_t;

